{
  "name": "uav360",
  "version": "1.2.0",
  "private": true,
  "description": "Browser KLV decoder and Cesium UAV video demo",
  "license": "Apache-2.0",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "relay": "node websocket-relay.js",
    "dev": "node scripts/start.js",
    "demo": "node scripts/start.js --demo",
    "test": "node --test test/*.test.js",
    "test:browser": "playwright test",
    "check": "npm run build && npm test"
  },
  "dependencies": { "cesium": "1.145.0", "ws": "8.21.3" },
  "devDependencies": { "esbuild": "0.28.2", "@playwright/test": "^1.58.0" }
}
